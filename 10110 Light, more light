#include<bits/stdc++.h>
using namespace std;
int main()
{
   long long int n,num;
    while(cin>>n )
    {
        if(n==0) break;
        num=sqrt(n);
        if(num*num==n) cout<<"yes"<<endl;
        else cout<<"no"<<endl;
    }
}
